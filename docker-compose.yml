services:
  backend:
    build:
      context: ./backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: "file:./data/opal.sqlite"
    volumes:
      - backend_data:/app/data

volumes:
  backend_data:
